node_modules/
dist/
public/data/
.DS_Store

# Raw source workbook only — NOT data/, which now IS tracked (see below):
# the deployed build (Vercel's prebuild "sync-data" script) reads
# data/bookings.json directly, so it has to exist in the repo, not just
# locally. Re-run node etl.cjs against your own copy of the workbook to
# regenerate data/*.json, then commit the result.
*.xlsx
